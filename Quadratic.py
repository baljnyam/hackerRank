import math


def find_roots(a, b, c):
    t = (-b + math.sqrt(b**2 - 4*a*c))/2*a
    -10 + 100 - 4*2*8
    m = (-b - math.sqrt(b**2 - 4*a*c))/2*a
    return t, m, -b


print(find_roots(2, 10, 8))
